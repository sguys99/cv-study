# Hugging Face 튜토리얼 Part 5.1: HF Agents / smolagents

## 학습 목표
- smolagents 프레임워크 이해 및 설치
- Tool 정의 방법 (@tool 데코레이터, Tool 클래스)
- CodeAgent vs ToolCallingAgent 차이점
- Multi-Agent 시스템 구축

---

## 1. smolagents 소개

### 1.1 smolagents란?

- **Hugging Face**에서 개발한 경량 AI Agent 프레임워크
- 핵심 로직이 **~1,000줄**의 코드로 구성 (최소한의 추상화)
- **Code Agent**: LLM이 Python 코드를 직접 작성하여 실행
- 2024년 12월 출시, transformers.agents의 후속

```
┌─────────────────────────────────────────────────────────────┐
│                     smolagents 특징                          │
├─────────────────────────────────────────────────────────────┤
│  ✨ Simplicity     : ~1,000 lines of code                   │
│  🧑‍💻 Code-First    : Python 코드로 액션 실행                 │
│  🤗 Hub Integration: Tool/Agent 공유 및 로드                 │
│  🌐 Model-Agnostic : HF, OpenAI, Anthropic 등 지원          │
│  👁️ Modality-Agnostic: Text, Vision, Audio 지원            │
│  🛠️ Tool-Agnostic  : MCP, LangChain, Hub Space 지원         │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 Agent 동작 원리 (ReAct 패턴)

```
┌─────────────────────────────────────────────────────────────┐
│                    ReAct Loop                               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   Task ──▶ [Think] ──▶ [Act] ──▶ [Observe] ──┐             │
│              │                                 │             │
│              └─────────── Loop ◀──────────────┘             │
│                          │                                  │
│                          ▼                                  │
│                   [Final Answer]                            │
└─────────────────────────────────────────────────────────────┘
```

1. **Think**: 문제 분석 및 계획 수립
2. **Act**: Tool 호출 또는 코드 실행
3. **Observe**: 결과 관찰 및 평가
4. **Repeat**: 목표 달성까지 반복

### 1.3 설치

```bash
# 기본 설치
pip install smolagents

# 추가 기능 포함 설치
pip install "smolagents[toolkit]"      # 기본 도구 포함
pip install "smolagents[litellm]"      # LiteLLM 지원
pip install "smolagents[transformers]" # 로컬 모델 지원
pip install "smolagents[gradio]"       # Gradio UI 지원
pip install "smolagents[mcp]"          # MCP 서버 지원

# 전체 설치
pip install "smolagents[all]"
```

### 1.4 HuggingFace 로그인

```python
from huggingface_hub import login

# 토큰으로 로그인 (https://hf.co/settings/tokens)
login("YOUR_HF_TOKEN")
```

---

## 2. 첫 번째 Agent 만들기

### 2.1 기본 Agent 생성

```python
from smolagents import CodeAgent, InferenceClientModel

# 모델 초기화 (HF Inference API 사용)
model = InferenceClientModel()  # 기본 모델 사용

# Agent 생성
agent = CodeAgent(tools=[], model=model)

# 실행
result = agent.run("1부터 10까지의 합을 계산해줘")
print(result)  # 55
```

### 2.2 Tool 추가하기

```python
from smolagents import CodeAgent, InferenceClientModel, DuckDuckGoSearchTool

# 모델 초기화
model = InferenceClientModel()

# 검색 도구와 함께 Agent 생성
agent = CodeAgent(
    tools=[DuckDuckGoSearchTool()],
    model=model,
)

# 웹 검색이 필요한 질문
result = agent.run("현재 대한민국 대통령은 누구인가요?")
print(result)
```

### 2.3 실행 결과 예시

```
╭──────────────────────────────── New run ────────────────────────────────╮
│ 현재 대한민국 대통령은 누구인가요?                                        │
╰─────────────────────────────────────────────────────────────────────────╯

=== Agent thoughts:
대한민국의 현재 대통령을 찾기 위해 웹 검색을 수행하겠습니다.

>>> Agent is executing the code below:
search_results = web_search("대한민국 현재 대통령 2024")
print(search_results)

>>> Observation:
윤석열 대통령은 2022년 5월 10일 취임...

=== Agent thoughts:
검색 결과를 바탕으로 답변을 제공하겠습니다.

>>> Agent is executing the code below:
final_answer("현재 대한민국의 대통령은 윤석열입니다.")

Final answer: 현재 대한민국의 대통령은 윤석열입니다.
```

---

## 3. Model 설정

### 3.1 지원 모델 클래스

| 클래스 | 설명 | 요구사항 |
|--------|------|----------|
| `InferenceClientModel` | HF Inference API | HF Token |
| `LiteLLMModel` | 100+ LLM 지원 | API Key |
| `TransformersModel` | 로컬 모델 | GPU/CPU |
| `OpenAIModel` | OpenAI 호환 API | API Key |

### 3.2 InferenceClientModel (HF API)

```python
from smolagents import InferenceClientModel

# 기본 모델 (무료, Rate Limit 있음)
model = InferenceClientModel()

# 특정 모델 지정
model = InferenceClientModel(
    model_id="meta-llama/Llama-3.3-70B-Instruct",
    token="YOUR_HF_TOKEN",  # 선택적
)

# 다른 Provider 사용
model = InferenceClientModel(
    model_id="Qwen/Qwen2.5-72B-Instruct",
    provider="together",  # together, sambanova, hyperbolic 등
)
```

### 3.3 LiteLLMModel (100+ LLM)

```python
from smolagents import LiteLLMModel
import os

# OpenAI
model = LiteLLMModel(
    model_id="gpt-4o",
    api_key=os.environ["OPENAI_API_KEY"],
)

# Anthropic Claude
model = LiteLLMModel(
    model_id="anthropic/claude-3-5-sonnet-latest",
    api_key=os.environ["ANTHROPIC_API_KEY"],
    temperature=0.2,
)

# AWS Bedrock
model = LiteLLMModel(
    model_id="bedrock/anthropic.claude-3-sonnet-20240229-v1:0",
)
```

### 3.4 TransformersModel (로컬)

```python
from smolagents import TransformersModel

# 로컬 모델 로드
model = TransformersModel(
    model_id="Qwen/Qwen2.5-7B-Instruct",
    device_map="auto",
    max_new_tokens=4096,
)
```

### 3.5 OpenAIModel (OpenAI 호환)

```python
from smolagents import OpenAIModel
import os

# OpenAI 직접 사용
model = OpenAIModel(
    model_id="gpt-4o",
    api_key=os.environ["OPENAI_API_KEY"],
)

# OpenRouter 사용
model = OpenAIModel(
    model_id="openai/gpt-4o",
    api_base="https://openrouter.ai/api/v1",
    api_key=os.environ["OPENROUTER_API_KEY"],
)

# Together AI 사용
model = OpenAIModel(
    model_id="meta-llama/Llama-3.3-70B-Instruct-Turbo",
    api_base="https://api.together.xyz/v1",
    api_key=os.environ["TOGETHER_API_KEY"],
)
```

---

## 4. Tool 정의하기

### 4.1 Tool의 구성 요소

Tool은 LLM이 호출할 수 있는 함수로, 다음 정보가 필요:

| 요소 | 설명 |
|------|------|
| `name` | 도구 이름 |
| `description` | 도구 설명 (LLM이 언제 사용할지 판단) |
| `inputs` | 입력 파라미터 (타입, 설명) |
| `output_type` | 출력 타입 |

### 4.2 @tool 데코레이터 (간단한 방법)

```python
from smolagents import tool, CodeAgent, InferenceClientModel


@tool
def get_weather(city: str) -> str:
    """
    주어진 도시의 현재 날씨 정보를 반환합니다.
    
    Args:
        city: 날씨를 조회할 도시 이름
    
    Returns:
        날씨 정보 문자열
    """
    # 실제로는 API 호출
    weather_data = {
        "서울": "맑음, 15°C",
        "부산": "흐림, 18°C",
        "제주": "비, 20°C",
    }
    return weather_data.get(city, f"{city}의 날씨 정보를 찾을 수 없습니다.")


@tool
def calculate_bmi(weight_kg: float, height_cm: float) -> str:
    """
    체중과 키를 기반으로 BMI를 계산합니다.
    
    Args:
        weight_kg: 체중 (kg)
        height_cm: 키 (cm)
    
    Returns:
        BMI 값과 판정 결과
    """
    height_m = height_cm / 100
    bmi = weight_kg / (height_m ** 2)
    
    if bmi < 18.5:
        status = "저체중"
    elif bmi < 25:
        status = "정상"
    elif bmi < 30:
        status = "과체중"
    else:
        status = "비만"
    
    return f"BMI: {bmi:.1f} ({status})"


# Agent에 Tool 추가
model = InferenceClientModel()
agent = CodeAgent(
    tools=[get_weather, calculate_bmi],
    model=model,
)

# 실행
result = agent.run("서울의 날씨를 알려주고, 키 175cm 몸무게 70kg인 사람의 BMI를 계산해줘")
print(result)
```

### 4.3 Tool 클래스 (고급 방법)

```python
from smolagents import Tool
from huggingface_hub import list_models


class HFModelDownloadsTool(Tool):
    """HuggingFace Hub에서 가장 많이 다운로드된 모델 조회"""
    
    name = "model_download_counter"
    description = """
    주어진 태스크에서 HuggingFace Hub에서 가장 많이 다운로드된 모델을 반환합니다.
    태스크 예시: 'text-generation', 'image-classification', 'text-to-image' 등
    """
    inputs = {
        "task": {
            "type": "string",
            "description": "모델을 검색할 태스크 종류",
        }
    }
    output_type = "string"
    
    def forward(self, task: str) -> str:
        """Tool 실행 로직"""
        try:
            models = list_models(filter=task, sort="downloads", direction=-1)
            top_model = next(iter(models))
            return f"가장 인기 있는 {task} 모델: {top_model.id} (다운로드: {top_model.downloads:,})"
        except StopIteration:
            return f"'{task}' 태스크에 해당하는 모델을 찾을 수 없습니다."


# Tool 인스턴스 생성 및 사용
model_tool = HFModelDownloadsTool()

agent = CodeAgent(
    tools=[model_tool],
    model=InferenceClientModel(),
)

result = agent.run("text-to-image 태스크에서 가장 인기 있는 모델은 뭐야?")
print(result)
```

### 4.4 상태를 가진 Tool

```python
from smolagents import Tool
from typing import List


class TodoListTool(Tool):
    """할 일 목록 관리 도구"""
    
    name = "todo_manager"
    description = "할 일 목록을 관리합니다. add, list, remove, clear 액션을 지원합니다."
    inputs = {
        "action": {
            "type": "string",
            "description": "수행할 액션: 'add', 'list', 'remove', 'clear'",
        },
        "item": {
            "type": "string",
            "description": "추가하거나 삭제할 항목 (add, remove 시 필요)",
            "nullable": True,
        }
    }
    output_type = "string"
    
    def __init__(self, **kwargs):
        super().__init__(**kwargs)
        self.todos: List[str] = []
    
    def forward(self, action: str, item: str = None) -> str:
        if action == "add":
            if item:
                self.todos.append(item)
                return f"'{item}' 추가됨. 현재 {len(self.todos)}개 항목"
            return "추가할 항목을 지정해주세요."
        
        elif action == "list":
            if not self.todos:
                return "할 일 목록이 비어있습니다."
            return "할 일 목록:\n" + "\n".join(f"- {t}" for t in self.todos)
        
        elif action == "remove":
            if item and item in self.todos:
                self.todos.remove(item)
                return f"'{item}' 삭제됨"
            return f"'{item}'을(를) 찾을 수 없습니다."
        
        elif action == "clear":
            self.todos.clear()
            return "모든 항목이 삭제되었습니다."
        
        return f"알 수 없는 액션: {action}"


# 사용
todo_tool = TodoListTool()
agent = CodeAgent(tools=[todo_tool], model=InferenceClientModel())

agent.run("할 일 목록에 '보고서 작성', '이메일 확인', '미팅 참석'을 추가해줘")
agent.run("현재 할 일 목록을 보여줘")
```

### 4.5 Hub에서 Tool 로드

```python
from smolagents import load_tool, CodeAgent, InferenceClientModel

# Hub에서 Tool 로드
image_tool = load_tool(
    "m-ric/text-to-image",
    trust_remote_code=True,
)

agent = CodeAgent(
    tools=[image_tool],
    model=InferenceClientModel(),
)

result = agent.run("우주에서 본 지구 이미지를 생성해줘")
```

### 4.6 Hub Space를 Tool로 사용

```python
from smolagents import Tool, CodeAgent, InferenceClientModel

# Gradio Space를 Tool로 변환
image_generation_tool = Tool.from_space(
    "black-forest-labs/FLUX.1-schnell",
    name="image_generator",
    description="프롬프트로 이미지를 생성합니다",
)

agent = CodeAgent(
    tools=[image_generation_tool],
    model=InferenceClientModel(),
)

result = agent.run("귀여운 고양이 이미지를 생성해줘")
```

### 4.7 LangChain Tool 사용

```python
from smolagents import Tool, CodeAgent, InferenceClientModel
from langchain.agents import load_tools

# LangChain Tool을 smolagents Tool로 변환
search_tool = Tool.from_langchain(
    load_tools(["serpapi"])[0]
)

agent = CodeAgent(
    tools=[search_tool],
    model=InferenceClientModel(),
)

result = agent.run("BERT 인코더는 몇 개의 레이어를 가지고 있나요?")
```

### 4.8 Tool을 Hub에 공유

```python
from smolagents import tool


@tool
def reverse_text(text: str) -> str:
    """텍스트를 뒤집습니다.
    
    Args:
        text: 뒤집을 텍스트
    
    Returns:
        뒤집힌 텍스트
    """
    return text[::-1]


# Hub에 업로드
reverse_text.push_to_hub("your-username/reverse-text-tool")
```

---

## 5. CodeAgent vs ToolCallingAgent

### 5.1 핵심 차이점

```
┌─────────────────────────────────────────────────────────────┐
│                     CodeAgent                                │
├─────────────────────────────────────────────────────────────┤
│  LLM ──▶ [Python 코드 생성] ──▶ [코드 실행] ──▶ 결과        │
│                                                             │
│  예시:                                                       │
│  result = web_search("날씨 서울")                            │
│  temperature = parse_temperature(result)                    │
│  final_answer(f"서울 온도: {temperature}°C")                │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                   ToolCallingAgent                          │
├─────────────────────────────────────────────────────────────┤
│  LLM ──▶ [JSON Tool Call 생성] ──▶ [Tool 실행] ──▶ 결과     │
│                                                             │
│  예시:                                                       │
│  {                                                          │
│    "tool": "web_search",                                    │
│    "arguments": {"query": "날씨 서울"}                       │
│  }                                                          │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 비교표

| 특성 | CodeAgent | ToolCallingAgent |
|------|-----------|------------------|
| **액션 형식** | Python 코드 | JSON/Text |
| **표현력** | 높음 (루프, 조건문, 변수) | 제한적 |
| **유연성** | 동적 로직 가능 | 사전 정의된 액션만 |
| **보안 위험** | 높음 (코드 실행) | 낮음 |
| **디버깅** | 복잡할 수 있음 | 예측 가능 |
| **효율성** | ~30% 적은 스텝 | 더 많은 스텝 필요 |
| **권장 용도** | 복잡한 연산, 데이터 처리 | 간단한 API 호출 |

### 5.3 CodeAgent 사용

```python
from smolagents import CodeAgent, InferenceClientModel, DuckDuckGoSearchTool

model = InferenceClientModel()

# CodeAgent: Python 코드로 액션 실행
code_agent = CodeAgent(
    tools=[DuckDuckGoSearchTool()],
    model=model,
    add_base_tools=True,  # 기본 도구 추가
)

# 복잡한 연산이 필요한 태스크
result = code_agent.run("""
피보나치 수열의 처음 20개 숫자를 계산하고,
그 중 짝수인 것들의 합을 구해줘.
""")
print(result)
```

**CodeAgent 실행 예시:**
```python
# Agent가 생성한 코드
def fibonacci(n):
    fib = [0, 1]
    for i in range(2, n):
        fib.append(fib[-1] + fib[-2])
    return fib

fib_20 = fibonacci(20)
even_sum = sum(x for x in fib_20 if x % 2 == 0)
final_answer(f"피보나치 처음 20개 중 짝수의 합: {even_sum}")
```

### 5.4 ToolCallingAgent 사용

```python
from smolagents import ToolCallingAgent, InferenceClientModel, tool


@tool
def get_stock_price(symbol: str) -> str:
    """주식 가격을 조회합니다.
    
    Args:
        symbol: 주식 심볼 (예: AAPL, GOOGL)
    
    Returns:
        현재 주가 정보
    """
    # 실제로는 API 호출
    prices = {"AAPL": 175.50, "GOOGL": 140.25, "MSFT": 380.00}
    price = prices.get(symbol.upper())
    if price:
        return f"{symbol}: ${price}"
    return f"{symbol} 정보를 찾을 수 없습니다."


model = InferenceClientModel()

# ToolCallingAgent: JSON 기반 Tool 호출
tool_agent = ToolCallingAgent(
    tools=[get_stock_price],
    model=model,
)

result = tool_agent.run("애플과 구글의 현재 주가를 알려줘")
print(result)
```

### 5.5 CodeAgent 추가 설정

```python
from smolagents import CodeAgent, InferenceClientModel

model = InferenceClientModel()

agent = CodeAgent(
    tools=[],
    model=model,
    # 추가 Python 라이브러리 허용
    additional_authorized_imports=["requests", "bs4", "pandas", "numpy"],
    # 최대 실행 스텝
    max_steps=10,
    # 기본 도구 추가 (web_search, python_interpreter 등)
    add_base_tools=True,
    # 출력 스트리밍
    stream_outputs=True,
)

# 웹 스크래핑 예시
result = agent.run("""
https://huggingface.co/blog 페이지에서 
첫 번째 블로그 포스트의 제목을 가져와줘.
requests와 bs4를 사용해.
""")
```

### 5.6 E2B 샌드박스 실행 (보안)

```python
import os
from smolagents import CodeAgent, InferenceClientModel

# E2B API 키 설정
os.environ["E2B_API_KEY"] = "your-e2b-api-key"

model = InferenceClientModel()

# E2B 샌드박스에서 코드 실행
agent = CodeAgent(
    tools=[],
    model=model,
    use_e2b_executor=True,  # E2B 샌드박스 사용
)

result = agent.run("시스템 정보를 출력해줘")
```

---

## 6. Multi-Agent 시스템

### 6.1 ManagedAgent 개념

```
┌─────────────────────────────────────────────────────────────┐
│                   Manager Agent                             │
│                        │                                    │
│           ┌───────────┼───────────┐                        │
│           │           │           │                        │
│           ▼           ▼           ▼                        │
│     [Web Agent]  [Code Agent]  [Research Agent]            │
│           │           │           │                        │
│           └───────────┼───────────┘                        │
│                       │                                    │
│                       ▼                                    │
│                 Final Answer                               │
└─────────────────────────────────────────────────────────────┘
```

### 6.2 기본 Multi-Agent 구현

```python
from smolagents import (
    CodeAgent,
    ToolCallingAgent,
    ManagedAgent,
    InferenceClientModel,
    DuckDuckGoSearchTool,
    tool,
)


# 모델 초기화
model = InferenceClientModel()


# 1. 웹 검색 전문 Agent
web_agent = CodeAgent(
    tools=[DuckDuckGoSearchTool()],
    model=model,
)

# ManagedAgent로 래핑
managed_web_agent = ManagedAgent(
    agent=web_agent,
    name="web_search_agent",
    description="웹에서 정보를 검색합니다. 검색 쿼리를 인자로 전달하세요.",
)


# 2. 계산 전문 Agent
@tool
def calculator(expression: str) -> str:
    """수학 표현식을 계산합니다.
    
    Args:
        expression: 계산할 수학 표현식
    
    Returns:
        계산 결과
    """
    try:
        result = eval(expression)
        return str(result)
    except Exception as e:
        return f"계산 오류: {e}"


calc_agent = CodeAgent(
    tools=[calculator],
    model=model,
)

managed_calc_agent = ManagedAgent(
    agent=calc_agent,
    name="calculator_agent",
    description="수학 계산을 수행합니다. 수학 표현식을 전달하세요.",
)


# 3. Manager Agent
manager_agent = CodeAgent(
    tools=[],
    model=model,
    managed_agents=[managed_web_agent, managed_calc_agent],
)

# 실행
result = manager_agent.run("""
대한민국의 인구수를 검색하고,
그 인구를 1000으로 나눈 값을 계산해줘.
""")
print(result)
```

### 6.3 전문화된 Agent 팀

```python
from smolagents import CodeAgent, ManagedAgent, InferenceClientModel, tool


model = InferenceClientModel()


# 번역 Agent
@tool
def translate(text: str, target_lang: str) -> str:
    """텍스트를 번역합니다.
    
    Args:
        text: 번역할 텍스트
        target_lang: 목표 언어 (예: 'en', 'ko', 'ja')
    
    Returns:
        번역된 텍스트
    """
    # 실제로는 번역 API 호출
    return f"[{target_lang}로 번역됨] {text}"


translator_agent = CodeAgent(tools=[translate], model=model)
managed_translator = ManagedAgent(
    agent=translator_agent,
    name="translator",
    description="텍스트를 다른 언어로 번역합니다.",
)


# 요약 Agent
@tool
def summarize(text: str, max_sentences: int = 3) -> str:
    """텍스트를 요약합니다.
    
    Args:
        text: 요약할 텍스트
        max_sentences: 최대 문장 수
    
    Returns:
        요약된 텍스트
    """
    sentences = text.split(".")[:max_sentences]
    return ". ".join(sentences) + "."


summarizer_agent = CodeAgent(tools=[summarize], model=model)
managed_summarizer = ManagedAgent(
    agent=summarizer_agent,
    name="summarizer",
    description="긴 텍스트를 짧게 요약합니다.",
)


# Orchestrator Agent
orchestrator = CodeAgent(
    tools=[],
    model=model,
    managed_agents=[managed_translator, managed_summarizer],
    planning_interval=3,  # 3스텝마다 계획 재수립
)

result = orchestrator.run("""
다음 영어 텍스트를 한국어로 번역하고, 2문장으로 요약해줘:
"Artificial intelligence is transforming how we work and live. 
Machine learning models can now understand natural language, 
generate images, and even write code. The possibilities are endless,
but we must also consider the ethical implications of these technologies."
""")
print(result)
```

---

## 7. 기본 제공 Tool

### 7.1 내장 Tool 목록

```python
from smolagents import CodeAgent, InferenceClientModel

model = InferenceClientModel()

# add_base_tools=True로 기본 도구 추가
agent = CodeAgent(
    tools=[],
    model=model,
    add_base_tools=True,
)

# 추가되는 기본 도구들:
# - DuckDuckGoSearchTool: 웹 검색
# - PythonInterpreterTool: Python 코드 실행 (ToolCallingAgent용)
# - Transcriber: 음성→텍스트 변환 (Whisper)
```

### 7.2 DuckDuckGoSearchTool

```python
from smolagents import DuckDuckGoSearchTool

search_tool = DuckDuckGoSearchTool()

# 직접 사용
result = search_tool("Python 3.12 새로운 기능")
print(result)
```

### 7.3 VisitWebpageTool

```python
from smolagents import VisitWebpageTool

webpage_tool = VisitWebpageTool()

# 웹페이지 내용 가져오기
content = webpage_tool("https://huggingface.co/blog")
print(content[:500])
```

### 7.4 Tool Collection 사용

```python
from smolagents import ToolCollection, CodeAgent, InferenceClientModel

# Hub에서 Tool Collection 로드
image_tools = ToolCollection(
    collection_slug="huggingface-tools/diffusion-tools-6630bb19a942c2306a2cdb6f",
    token="YOUR_HF_TOKEN",
)

agent = CodeAgent(
    tools=[*image_tools.tools],
    model=InferenceClientModel(),
    add_base_tools=True,
)

result = agent.run("강과 호수가 있는 풍경 이미지를 생성해줘")
```

---

## 8. Gradio UI 통합

### 8.1 기본 UI

```python
from smolagents import CodeAgent, InferenceClientModel, DuckDuckGoSearchTool
from smolagents import GradioUI

model = InferenceClientModel()
agent = CodeAgent(
    tools=[DuckDuckGoSearchTool()],
    model=model,
)

# Gradio UI 실행
GradioUI(agent).launch()
```

### 8.2 커스텀 UI 설정

```python
from smolagents import GradioUI

# 커스텀 설정으로 UI 실행
ui = GradioUI(
    agent,
    file_upload_folder="./uploads",  # 파일 업로드 폴더
)
ui.launch(
    share=True,       # 공개 링크 생성
    server_port=7860, # 포트 설정
)
```

---

## 9. Agent를 Hub에 공유

### 9.1 Agent 저장 및 로드

```python
from smolagents import CodeAgent, InferenceClientModel, tool


@tool
def my_custom_tool(text: str) -> str:
    """커스텀 도구입니다."""
    return f"처리됨: {text}"


# Agent 생성
agent = CodeAgent(
    tools=[my_custom_tool],
    model=InferenceClientModel(),
)

# Hub에 업로드
agent.push_to_hub("your-username/my-awesome-agent")

# Hub에서 로드
from smolagents import load_agent

loaded_agent = load_agent("your-username/my-awesome-agent")
result = loaded_agent.run("테스트 실행")
```

---

## 10. 실습 과제

### 과제 1: 날씨 + 번역 Agent
1. 날씨 조회 Tool 구현 (@tool 데코레이터)
2. 번역 Tool 구현 (한영/영한)
3. CodeAgent로 "서울 날씨를 영어로 알려줘" 처리

### 과제 2: 데이터 분석 Agent
1. CSV 파일 읽기 Tool 구현 (Tool 클래스)
2. 기본 통계 계산 Tool 구현
3. CodeAgent로 데이터 분석 파이프라인 구축

### 과제 3: Multi-Agent 연구 보조
1. 웹 검색 Agent (ManagedAgent)
2. 요약 Agent (ManagedAgent)
3. Manager Agent로 "최신 LLM 트렌드 리서치" 수행

---

## 11. 요약 체크리스트

### Agent 기본
- [ ] smolagents 설치 및 설정
- [ ] InferenceClientModel, LiteLLMModel 사용
- [ ] CodeAgent 기본 실행
- [ ] Tool 추가 및 실행

### Tool 정의
- [ ] @tool 데코레이터 사용
- [ ] Tool 클래스 상속
- [ ] Hub에서 Tool 로드 (load_tool)
- [ ] Space를 Tool로 변환 (Tool.from_space)
- [ ] LangChain Tool 변환 (Tool.from_langchain)
- [ ] Tool Hub에 공유 (push_to_hub)

### Agent 타입
- [ ] CodeAgent: Python 코드 실행
- [ ] ToolCallingAgent: JSON 기반 Tool 호출
- [ ] 보안 설정 (E2B, additional_authorized_imports)

### Multi-Agent
- [ ] ManagedAgent 생성
- [ ] Manager Agent 구성
- [ ] Agent 계층 구조 설계

---

## 참고 자료

### 공식 문서
- **smolagents GitHub**: https://github.com/huggingface/smolagents
- **smolagents Documentation**: https://huggingface.co/docs/smolagents
- **smolagents Blog**: https://huggingface.co/blog/smolagents

### 튜토리얼
- **HF Agents Course**: https://huggingface.co/learn/agents-course
- **HF Cookbook - Agents**: https://huggingface.co/learn/cookbook/en/agents
- **Guided Tour**: https://smolagents.org/docs/agents-guided-tour/

### 모델
- **InferenceClientModel 지원 모델**: https://huggingface.co/models?inference=warm
- **LiteLLM 지원 모델**: https://docs.litellm.ai/docs/providers

---

## 다음 단계

**Part 5.2: Custom Tool Development**에서 다룰 내용:
- Vision/Search/Code 실행 Tool 심화
- MCP (Model Context Protocol) 통합
- RAG Tool 구현
- 프로덕션 배포 고려사항